<% @messages.each do |message| %>
  <% if message.user_id == current_user.id %>
    <%= render template: 'messages/me_template', layout: false, locals: { message: message } %>
  <% else %>
    <%= render template: 'messages/friend_template', layout: false, locals: { message: message } %>
  <% end %>
<% end %>